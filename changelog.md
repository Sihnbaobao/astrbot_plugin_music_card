## v1.2.3

- 卡片发送失败时不再重复发原链接,改为降级为带歌名/歌手的文字消息(原链接已在群里,避免"发链接又回链接"的废话)
- 无歌曲信息可用时放弃降级发送,只记录日志

## v1.2.2

- 修复:网易云/QQ音乐卡片发送失败(平台报"消息体无法解析"等)时不再抛错,自动兜底改发歌曲链接
- 发卡片前先校验歌曲 ID 是否存在,避免把歌单/用户 ID 误当歌曲 ID 发出无效卡片
- `send_song_card` 发送失败时如实返回失败说明,不再谎报"已发送"

## v1.2.1

- 音乐分享卡片不再重复发卡,而是把卡片中的歌名/歌手/链接改写进消息文本,让 LLM 能识别歌曲信息(别人发卡片后问"是什么歌"也能答上来)
- 仅纯文本音乐链接才会转换并发卡片

## v1.2.0

- 支持解析 QQ 分享卡片/网易云卡片(JSON 消息段)中的音乐链接,私人号 bot 也能处理卡片消息
- 修复专辑链接被误识别为歌曲 ID 的 bug(`userid` 被当成歌曲 id)

## v1.1.7

- 新增 `.editorconfig` 规范代码格式
- 同步 `main.py` 中 `@register` 版本号与 `metadata.yaml` 一致
- 清理冗余的 QQ 音乐子域名判断（`c6.y.qq.com`/`i.y.qq.com` 已被 `y.qq.com` 覆盖）

## v1.1.6

- 恢复插件层概率把关（35% 拒发），拒绝台词为"心情式"不评价歌曲
- 工具描述警告 AI"可能因心情拒发，别提前承诺"，避免"说喜欢又拒绝"的矛盾

## v1.1.5

- 调整 `send_song_card` 描述措辞，明确"不是真正喜欢就不调用"，减少 AI 被动服从

## v1.1.4

- 移除插件随机拒绝，发不发由 LLM 自己判断：真正喜欢的歌才分享，不合口味可直接拒绝
- 工具描述去除"使唤"等字眼，避免影响 AI 台词

## v1.1.3

- 拒绝台词改为"心情式"随机短语（不想发/没心情/不想动），不再评价歌曲本身，避免喜欢歌时显得矛盾

## v1.1.2

- 新增纯随缘意愿判定：`send_song_card` 每次调用有 30% 概率不想发，不再是"有求必应"
- 拒绝概率可在 `main.py` 的 `_refuse_rate` 调整

## v1.1.1

- 优化 LLM 工具描述，支持 bot 在聊天中主动搜歌/分享歌曲
- 工具明确"仅返回搜索结果，不会发送"，发卡片前由 LLM 自行判断是否分享

## v1.1.0

- 新增 LLM 工具 `search_songs` + `send_song_card`，bot 可自主搜歌发卡片
- 搜歌/发歌硬限制 + 30 秒重置
- 工具拒绝语含 bot 人格

## v1.0.0

- QQ/酷狗/网易云链接自动转网易云 type:163 卡片